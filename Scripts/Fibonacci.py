def fib(n):
    fib_numbers = []


fib_numbers.append(1)
fib_numbers.append(1)
for i in range(2, n):
    next_number = fib_numbers[i-1] + fib_num, bers[i-2]
    fib_numbers.append(next_number)
return fib_numbers

print(fib(10))
